<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Шифрование Цезаря" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Ввод текста -->
        <Label Grid.Row="0" Grid.Column="0" Content="Введите текст:"/>
        <TextBox x:Name="InputTextBox" Grid.Row="0" Grid.Column="1" Margin="5" ToolTip="Введите текст для шифрования или дешифрования"/>

        <!-- Ввод сдвига -->
        <Label Grid.Row="1" Grid.Column="0" Content="Сдвиг:"/>
        <TextBox x:Name="ShiftTextBox" Grid.Row="1" Grid.Column="1" Margin="5" Width="100" ToolTip="Введите целое число сдвига"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="5">
            <RadioButton x:Name="EncryptRadio" Content="Шифрование" IsChecked="True" Margin="0,0,10,0"/>
            <RadioButton x:Name="DecryptRadio" Content="Дешифрование"/>
        </StackPanel>

        <!-- Кнопка выполнить -->
        <Button Grid.Row="3" Grid.Column="0" Content="Выполнить" Width="100" Margin="5" Click="OnExecuteClick"/>

        <!-- Результат -->
        <Label Grid.Row="3" Grid.Column="1" Content="Результат:" VerticalAlignment="Top"/>
        <TextBox x:Name="ResultTextBox" Grid.Row="3" Grid.Column="1" Margin="5" VerticalScrollBarVisibility="Auto" Height="150" IsReadOnly="True"/>
    </Grid>
</Window>